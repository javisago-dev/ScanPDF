name: scan_pdf
description: Aplicación para escanear documentos y generar PDFs
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Escaneo de documentos con detección de bordes
  cunning_document_scanner: ^1.3.0

  # Generación de PDFs
  pdf: ^3.11.1

  # Manejo de archivos
  path_provider: ^2.1.4

  # Compartir archivos
  share_plus: ^10.1.2

  # Permisos
  permission_handler: ^12.0.1

  # Internacionalización
  intl: ^0.20.2 # UI
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
